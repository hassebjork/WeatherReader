TODO
- Sensors: Store wind dir as NULL when speed = 0.0
- Startscripts in /etc/init.d and links to /etc/rc.X
- Reconnect database if connection reset/lost/broken
- Search for arduino's serial device
- test-sever: add timestamp

Wish list
- Config file: Move some config data into database?
